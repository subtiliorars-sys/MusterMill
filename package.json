{
  "name": "mustermill",
  "version": "0.2.0",
  "private": true,
  "description": "Mustermate idle spin-off — breed chibi soldiers, grow battalion strength",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:itch": "bash scripts/build-itch.sh",
    "release:itch": "bash scripts/push-itch.sh",
    "import:quips": "node scripts/import-branch-quips.mjs",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.7",
    "vitest": "^3.0.5"
  }
}
